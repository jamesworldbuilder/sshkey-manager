# Configuration variables for SSH Key Management Script (ssh_manager.sh)
# Required variables: SRC_DIR, SSH_DIR, KEY_COMMENT, KEY_TYPE, KEY_BITS
# Examples:
#   SRC_DIR=/path/to/source_directory/.ssh
#   SSH_DIR=/path/to/ssh_directory/.ssh
#   KEY_COMMENT=user@example.com
#   KEY_TYPE=rsa
#   KEY_BITS=4096

# Source Directory
# (where SSH keys will be copied from or generated in):
SRC_DIR=/media/user/sshkey-manager/.ssh 

# SSH Directory / "Destination" Directory 
# (where SSH keys will be copied to and where other servers or services will look for your SSH keys):
SSH_DIR=/home/user/.ssh 

# SSH Key Comment
# (provides additional information or context about the key):
KEY_COMMENT=user@example.com

# SSH Key Type
# (the cryptographic algorithm used to generate the SSH key):
KEY_TYPE=rsa

# SSH Key Bits
# (the size/length of the cryptographic key that determines the strength of its encryption)
KEY_BITS=4096
